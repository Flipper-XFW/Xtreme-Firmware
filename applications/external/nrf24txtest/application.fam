# For details & more options, see documentation/AppManifests.md in firmware repo

App(
    appid="nrf24txtest",  # Must be unique
    name="[NRF24] TX test",  # Displayed in menus
    apptype=FlipperAppType.EXTERNAL,
    entry_point="nrf24txtest_app",
    stack_size=2 * 1024,
    fap_category="GPIO",
    fap_icon="nrf24txtest.png",  # 10x10 1-bit PNG
    fap_private_libs=[
        Lib(
            name="nrf24",
            sources=[
                "nrf24.c",
            ],
        ),
    ],
)
